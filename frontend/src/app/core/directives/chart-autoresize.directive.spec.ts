import { ChartAutoresizeDirective } from './chart-autoresize.directive';

describe('ChartAutoresizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartAutoresizeDirective();
    expect(directive).toBeTruthy();
  });
});
